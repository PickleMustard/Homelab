apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: readarr-service
    app.kubernetes.io/name: readarr-service
  name: readarr-service
  namespace: media
spec:
  ports:
    - name: "8787"
      port: 8787
      targetPort: 8787
  selector:
    app.kubernetes.io/instance: servarr
    app.kubernetes.io/name: servarr
