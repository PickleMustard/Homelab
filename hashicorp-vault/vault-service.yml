apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: vault-service
    app.kubernetes.io/name: vault-service
  name: vault-service
  namespace: auth
spec:
  ports:
    - name: "http"
      port: 8200
      targetPort: 8200
      protocol: TCP
  selector:
    app.kubernetes.io/instance: vault
    app.kubernetes.io/name: vault
  type: ClusterIP
