apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: stalwart
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: stalwart
  namespaceSelector:
    matchNames:
      - stalwart
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
